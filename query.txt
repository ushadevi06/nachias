ALTER TABLE `uoms` DROP INDEX `uoms_uom_code_unique`;
ALTER TABLE `states` DROP INDEX `states_state_code_unique`;
ALTER TABLE `shipping_methods` DROP INDEX `shipping_methods_name_unique`;
ALTER TABLE `transport_modes` DROP INDEX `transport_modes_name_unique`;
ALTER TABLE `seasons` DROP INDEX `seasons_season_code_unique`;
ALTER TABLE `shifts` DROP INDEX `shifts_updated_by_foreign`;
ALTER TABLE `shifts` DROP INDEX `shifts_shift_name_unique`;
ALTER TABLE `shifts` DROP INDEX `shifts_created_by_foreign`;
ALTER TABLE `customers` DROP INDEX `customers_code_unique`;
ALTER TABLE `suppliers` DROP INDEX `suppliers_store_id_foreign`;
ALTER TABLE `suppliers` DROP INDEX `suppliers_code_unique`;
ALTER TABLE `purchase_commission_agents` DROP INDEX `purchase_commission_agents_code_unique`;
ALTER TABLE `service_providers` DROP INDEX `service_providers_operation_stage_id_foreign`;
ALTER TABLE `sales_agents` DROP INDEX `sales_agents_zone_id_foreign`;
ALTER TABLE `sales_agents` DROP INDEX `sales_agents_code_unique`;
ALTER TABLE `sales_agents` DROP INDEX `sales_agents_state_id_foreign`;
ALTER TABLE `sales_agents` DROP INDEX `sales_agents_city_id_foreign`;
ALTER TABLE `sales_agents` DROP INDEX `sales_agents_place_id_foreign`;
ALTER TABLE `store_categories` DROP INDEX `store_categories_code_unique`;
ALTER TABLE `brand_categories` DROP INDEX `brand_categories_code_unique`;
ALTER TABLE `items` DROP INDEX `items_product_barcode_unique`;
ALTER TABLE `items` DROP INDEX `items_code_unique`;

DROP TABLE IF EXISTS `item_prices`;
CREATE TABLE  `item_prices` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `finished_item_code` varchar(255) DEFAULT NULL,
  `art_no` varchar(255) DEFAULT NULL,
  `unit_price` decimal(10,2) NOT NULL,
  `selling_price` decimal(10,2) NOT NULL,
  `effective_from` date NOT NULL,
  `status` enum('Active','Inactive') NOT NULL DEFAULT 'Active',
  `created_by` bigint(20) unsigned DEFAULT NULL,
  `updated_by` bigint(20) unsigned DEFAULT NULL,
  `deleted_at` timestamp NULL DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

DROP TABLE IF EXISTS `sales_order_charges`;
CREATE TABLE `sales_order_charges` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `sales_order_id` bigint(20) unsigned NOT NULL,
  `charge_id` bigint(20) unsigned DEFAULT NULL,
  `charge_name` varchar(255) DEFAULT NULL,
  `charge_amount` decimal(15,2) NOT NULL DEFAULT 0.00,
  `tax_type` enum('Pre-GST','Post-GST') NOT NULL DEFAULT 'Post-GST',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `sales_order_charges_sales_order_id_foreign` (`sales_order_id`),
  CONSTRAINT `sales_order_charges_sales_order_id_foreign` FOREIGN KEY (`sales_order_id`) REFERENCES `sales_orders` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- sales_invoice_items
ALTER TABLE sales_invoice_items
ADD COLUMN color_id BIGINT UNSIGNED NULL AFTER item_id;


-- stock_entry_items
ALTER TABLE stock_entry_items
ADD COLUMN barcode TEXT NULL AFTER qrcode;

ALTER TABLE stock_entry_items
ADD COLUMN fit_id BIGINT UNSIGNED NULL AFTER fabric_type_id;


-- sales_orders
ALTER TABLE sales_orders ADD COLUMN orderaxe_id VARCHAR(255) NULL;

ALTER TABLE sales_orders
ADD COLUMN other_charges DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER tax_amount;

  ALTER TABLE sales_orders
  ADD COLUMN sales_discount_percent DECIMAL(5,2) NOT NULL DEFAULT 0 AFTER commission_amount;


-- sales_order_items
ALTER TABLE sales_order_items
DROP COLUMN uom_id;

ALTER TABLE sales_order_items
ADD COLUMN uom_id VARCHAR(255) NULL;

ALTER TABLE sales_order_items
ADD COLUMN sku VARCHAR(255) NULL AFTER stock_entry_item_id;



08-05:
ALTER TABLE `production_services` ADD `cost` DECIMAL(10,2) NULL AFTER `status`;

ALTER TABLE `job_card_entries`
ADD COLUMN `no_of_days` INT UNSIGNED NULL AFTER `delivery_date`;


ALTER TABLE `job_card_issue_items`
ADD COLUMN `barcode_no` VARCHAR(255) NULL AFTER `cost_per_pc`,
ADD COLUMN `qrcode_data` TEXT NULL AFTER `barcode_no`;



CREATE TABLE `barcode_masters` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `barcode_no` VARCHAR(255) NOT NULL UNIQUE,
    `art_no` VARCHAR(255) NULL,
    `item_name` VARCHAR(255) NULL,
    `sleeve_type` VARCHAR(255) NULL,
    `size` VARCHAR(255) NULL,
    `brand_id` BIGINT UNSIGNED NULL,
    `lot_no` VARCHAR(255) NULL,
    `color_id` BIGINT UNSIGNED NULL,
    `fabric_type_id` BIGINT UNSIGNED NULL,
    `created_at` TIMESTAMP NULL DEFAULT NULL,
    `updated_at` TIMESTAMP NULL DEFAULT NULL,
    PRIMARY KEY (`id`)
);


ALTER TABLE barcode_masters
ADD COLUMN item_code VARCHAR(255) NULL AFTER barcode_no;


ALTER TABLE barcode_masters
ADD COLUMN quantity DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER size;


ALTER TABLE barcode_masters
ADD COLUMN style_id BIGINT UNSIGNED NULL AFTER brand_id;


ALTER TABLE stock_entry_items;


ALTER TABLE job_card_entries
ADD COLUMN attachment VARCHAR(255) NULL AFTER status;


ALTER TABLE operation_stages
ADD COLUMN cost DECIMAL(10,2) NOT NULL DEFAULT 0 AFTER working_days;


ALTER TABLE sales_invoices
ADD COLUMN store_id BIGINT UNSIGNED NULL AFTER customer_id,
ADD COLUMN agent_id BIGINT UNSIGNED NULL AFTER store_id,
ADD COLUMN commission_percent DECIMAL(10,2) NOT NULL DEFAULT 0 AFTER discount,
ADD COLUMN commission_amount DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER commission_percent;


ALTER TABLE sales_invoices
ADD COLUMN transporter_name VARCHAR(255) NULL AFTER due_date,
ADD COLUMN lr_no VARCHAR(255) NULL AFTER transporter_name;

ALTER TABLE sales_invoices
ADD COLUMN no_of_box INT UNSIGNED NULL AFTER lr_no;

ALTER TABLE sales_invoice_items
DROP COLUMN brand_id,
DROP COLUMN item_id,
ADD COLUMN sku VARCHAR(255) NULL AFTER sales_invoice_id;



ALTER TABLE sales_invoice_items
MODIFY COLUMN uom_id VARCHAR(255) NULL;


ALTER TABLE raw_materials
ADD COLUMN standard_consumption_qty DECIMAL(10,4) NULL DEFAULT 0 AFTER min_stock,
DROP COLUMN supplier_design_name;


CREATE TABLE standard_consumptions (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    raw_material_id BIGINT UNSIGNED NOT NULL,
    fs_qty DECIMAL(10,4) NOT NULL DEFAULT 0,
    hs_qty DECIMAL(10,4) NOT NULL DEFAULT 0,
    created_by BIGINT UNSIGNED NULL,
    updated_by BIGINT UNSIGNED NULL,
    deleted_at TIMESTAMP NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    CONSTRAINT standard_consumptions_raw_material_id_foreign
        FOREIGN KEY (raw_material_id)
        REFERENCES raw_materials(id)
        ON DELETE CASCADE
);


ALTER TABLE raw_materials
DROP COLUMN standard_consumption_qty;


ALTER TABLE standard_consumptions
ADD COLUMN status ENUM('Active', 'Inactive') NOT NULL DEFAULT 'Active' AFTER hs_qty;


ALTER TABLE standard_consumptions 
MODIFY fs_qty DOUBLE NOT NULL DEFAULT 0;

ALTER TABLE standard_consumptions 
MODIFY hs_qty DOUBLE NOT NULL DEFAULT 0;


ALTER TABLE job_card_operations
ADD COLUMN rate DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER remarks,
ADD COLUMN total_cost DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER rate;

ALTER TABLE job_card_fabric_details
ADD COLUMN grn_image VARCHAR(255) NULL AFTER remaining_qty;


ALTER TABLE `settings` ADD `zip_code` VARCHAR(10) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL AFTER `address`;

ALTER TABLE `sales_invoices`
    ADD COLUMN `tran_doc_no` VARCHAR(255) NULL AFTER `transporter_name`,
    ADD COLUMN `tran_doc_date` VARCHAR(255) NULL AFTER `tran_doc_no`,
    ADD COLUMN `veh_type` VARCHAR(255) NULL AFTER `tran_doc_date`;

ALTER TABLE `sales_invoices`
    ADD COLUMN `irn` VARCHAR(64) NULL AFTER `invoice_status`,
    ADD COLUMN `ack_no` VARCHAR(255) NULL AFTER `irn`,
    ADD COLUMN `ack_date` DATETIME NULL AFTER `ack_no`,
    ADD COLUMN `signed_qr_code` TEXT NULL AFTER `ack_date`,
    ADD COLUMN `eway_bill_no` VARCHAR(255) NULL AFTER `signed_qr_code`,
    ADD COLUMN `eway_bill_date` DATETIME NULL AFTER `eway_bill_no`,
    ADD COLUMN `eway_bill_valid_till` DATETIME NULL AFTER `eway_bill_date`,
    ADD COLUMN `vehicle_no` VARCHAR(255) NULL AFTER `eway_bill_valid_till`,
    ADD COLUMN `transporter_id` VARCHAR(255) NULL AFTER `vehicle_no`,
    ADD COLUMN `transport_mode` VARCHAR(255) NULL AFTER `transporter_id`,
    ADD COLUMN `transport_distance` INT NULL AFTER `transport_mode`;

-- Add columns to settings table
ALTER TABLE `settings`
ADD COLUMN `bank_name` VARCHAR(255) NULL AFTER `so_prefix`,
ADD COLUMN `branch_location` VARCHAR(255) NULL AFTER `bank_name`,
ADD COLUMN `account_no` VARCHAR(255) NULL AFTER `branch_location`,
ADD COLUMN `ifsc_code` VARCHAR(255) NULL AFTER `account_no`;

-- Add column to sales_invoices table
ALTER TABLE `sales_invoices`
ADD COLUMN `hsn_sac` VARCHAR(50) NULL AFTER `no_of_box`;

ALTER TABLE `settings`
ADD COLUMN `qr_code` VARCHAR(255) NULL;

ALTER TABLE `sales_invoices`
ADD COLUMN `einvoice_status` VARCHAR(20) NULL AFTER `hsn_sac`;

ALTER TABLE stock_entry_items
ADD COLUMN style_id BIGINT UNSIGNED NULL,
ADD COLUMN fabric_width_id BIGINT UNSIGNED NULL,
ADD COLUMN brand_id BIGINT UNSIGNED NULL;

ALTER TABLE production_services
ADD COLUMN sequence INT NOT NULL DEFAULT 1
AFTER operation_stage_id;

ALTER TABLE production_receipt_items
ADD COLUMN mrp DECIMAL(15,2) DEFAULT 0 AFTER unit_price;

ALTER TABLE `production_receipt_items`
ADD COLUMN `mrp` DECIMAL(15,2) NOT NULL DEFAULT 0 AFTER `unit_price`;

ALTER TABLE `sales_order_items`
ADD COLUMN `commission_percent` DECIMAL(8,2) NOT NULL DEFAULT 0 AFTER `amount`,
ADD COLUMN `commission_amount` DECIMAL(12,2) NOT NULL DEFAULT 0 AFTER `commission_percent`;

ALTER TABLE settings
ADD COLUMN terms_and_conditions LONGTEXT NULL AFTER address;

ALTER TABLE `sales_orders` CHANGE `terms_conditions` `terms_conditions` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

ALTER TABLE `credit_note_items` DROP COLUMN `item_id`, DROP COLUMN `brand_category_id`, DROP COLUMN `size`, DROP COLUMN `sleeve_type`, DROP COLUMN `uom_id`;

-- credit_note_charges table
CREATE TABLE `credit_note_charges` (
    `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `credit_note_id` BIGINT UNSIGNED NOT NULL,
    `charge_id` BIGINT UNSIGNED NOT NULL,
    `charge_name` VARCHAR(255) NULL,
    `charge_amount` DECIMAL(15,2) NOT NULL DEFAULT 0.00,
    `tax_type` VARCHAR(255) NULL,
    `created_at` TIMESTAMP NULL,
    `updated_at` TIMESTAMP NULL,
    `deleted_at` TIMESTAMP NULL,
    CONSTRAINT `credit_note_charges_credit_note_id_foreign` 
        FOREIGN KEY (`credit_note_id`) REFERENCES `credit_notes`(`id`) ON DELETE CASCADE,
    CONSTRAINT `credit_note_charges_charge_id_foreign` 
        FOREIGN KEY (`charge_id`) REFERENCES `charges`(`id`)
);

-- Add columns to credit_notes
ALTER TABLE `credit_notes`
    ADD COLUMN `reason_detail` TEXT NULL AFTER `reason`,
    ADD COLUMN `zone_id` BIGINT UNSIGNED NULL AFTER `customer_id`,
    ADD COLUMN `agent_id` BIGINT UNSIGNED NULL AFTER `zone_id`,
    ADD COLUMN `show_fields` TEXT NULL AFTER `status`,
    ADD COLUMN `discount_percent` DECIMAL(5,2) NOT NULL DEFAULT 0.00 AFTER `sub_total`,
    ADD COLUMN `discount` DECIMAL(15,2) NOT NULL DEFAULT 0.00 AFTER `discount_percent`,
    ADD COLUMN `other_charges` DECIMAL(15,2) NOT NULL DEFAULT 0.00 AFTER `tax_amount`;

-- Add sales_invoice_ids to credit_notes
ALTER TABLE `credit_notes`
    ADD COLUMN `sales_invoice_ids` JSON NULL AFTER `sales_invoice_id`;

-- Add so_ids to sales_invoices and make so_id nullable
ALTER TABLE `sales_invoices`
    ADD COLUMN `so_ids` JSON NULL AFTER `so_id`,
    MODIFY COLUMN `so_id` BIGINT UNSIGNED NULL;