
07-09
-- 1. Job Card Entries Table
ALTER TABLE `job_card_entries`
    ADD COLUMN `additional_qty` INT NOT NULL DEFAULT 0 AFTER `grand_total_qty`;

-- 2. Job Card Fabric Details Table
ALTER TABLE `job_card_fabric_details`
    ADD COLUMN `is_additional` TINYINT NOT NULL DEFAULT 0 AFTER `job_card_entry_id`;

-- 3. Tasks Table
ALTER TABLE `tasks`
    ADD COLUMN `is_additional` TINYINT NOT NULL DEFAULT 0 AFTER `stage_id`,
    ADD COLUMN `job_card_fabric_detail_id` BIGINT UNSIGNED NULL AFTER `is_additional`;

-- 4. Production Receipts Table
ALTER TABLE `production_receipts`
    ADD COLUMN `is_additional` TINYINT NOT NULL DEFAULT 0 AFTER `job_card_id`,
    ADD COLUMN `job_card_fabric_detail_id` BIGINT UNSIGNED NULL AFTER `is_additional`;
